/**
 * Plugin and platform name constants.
 * PLUGIN_NAME must match package.json name.
 * PLATFORM_NAME must match config.schema.json pluginAlias.
 * @module settings
 */

export const PLATFORM_NAME = 'warpHomebridge';

export const PLUGIN_NAME = 'homebridge-warp-charger';
